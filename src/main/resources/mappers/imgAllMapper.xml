<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.resort.Sunset.mapper.ImgAllMapper">
    <select id="selectByRoomId" resultType="com.resort.Sunset.dto.img_all">
        SELECT * FROM img_all WHERE room_id = #{room_id}
    </select>
</mapper>